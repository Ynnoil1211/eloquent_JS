let theNumber = Number(prompt("Pick a number"));  // State change
if (!Number.isNaN(theNumber)) {                   // Conditional
  console.log("Your number is the square of " + theNumber * theNumber);
}
